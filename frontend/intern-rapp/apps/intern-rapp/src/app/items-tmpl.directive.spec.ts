import { ItemsTmplDirective } from './items-tmpl.directive';

describe('ItemsTmplDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemsTmplDirective();
    expect(directive).toBeTruthy();
  });
});
